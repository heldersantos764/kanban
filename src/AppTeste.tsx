import { FC } from "react";
import "./app.css";
import Login from "./pages/Login";

interface Props {}

const AppTeste: FC<Props> = (props) => {
  return (
    <Login/>
  );
};

export default AppTeste;